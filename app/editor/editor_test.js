'use strict';

describe('ieecloud-editor.editor module', function() {

  beforeEach(module('ieecloud-editor.editor'));

  describe('editor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('EditorCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});